import { CSSProperties } from "react";
import { LayoutWorkerHook } from "./useWorkerLayoutFactory";
declare type WorkerLayoutLabelKeys = "start" | "stop";
export interface WorkerLayoutControlProps<T> {
    id?: string;
    className?: string;
    style?: CSSProperties;
    layout: LayoutWorkerHook<T>;
    settings: T;
    autoRunFor?: number;
    children?: [JSX.Element, JSX.Element];
    labels?: {
        [Key in WorkerLayoutLabelKeys]?: string;
    };
}
export declare function WorkerLayoutControl<T>({ id, className, style, layout, settings, autoRunFor, children, labels, }: WorkerLayoutControlProps<T>): JSX.Element;
export {};
